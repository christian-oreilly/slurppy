name: slurppy_app_default_schema
type: map
allowempty: True
mapping:
  paths:
    allowempty: True
    required: True
    type: map
    mapping:
      log:
        pattern: .+
        required: True
        type: str
      slurm:
        pattern: .+
        required: True
        type: str
      tmp:
        pattern: .+
        required: True
        type: str
      output:
        pattern: .+
        required: True
        type: str
      venv:
        pattern: .+
        required: True
        type: str